---
name: {{name}}
description: "TODO - 中文为主：这个 skill 做什么 + 何时使用（含中文触发词）。English: one-line summary; triggers: keyword, keyword. Max 1024 chars."
license: MIT
metadata:
  category: TODO  # pick one: coding | document | writing | devops | data | research | productivity | meta
  tags: ""        # comma-separated, e.g. "git, workflow"
  origin: original
  author: lqzhgood
  version: "0.1"
  added: "{{date}}"
  updated: "{{date}}"
---

# {{name}}

TODO: instructions the agent follows when this skill is activated.

## When to use

TODO: the situations that should trigger this skill.

## Steps

1. TODO
2. TODO

## Edge cases

- TODO

<!--
Conventions (see docs/conventions.md):
- Keep this file under 500 lines; move long reference material into references/.
- Optional subdirectories: scripts/ (executable helpers), references/ (docs loaded on demand), assets/ (templates, images).
- No hardcoded user paths, emails, keys or internal hosts - run `npm run validate` before committing.
-->
